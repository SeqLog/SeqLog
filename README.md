# SeqLog

**A native Logseq for macOS.** An outliner that edits the Markdown on your disk — nothing in
between. Journal, backlinks, instant search and Git, in a SwiftUI app that opens before you
finish the keystroke. No Electron. No cloud. No lock-in.

**[Download for Mac →](https://seqlog.com/dl/latest/SeqLog.dmg)** · [seqlog.com](https://seqlog.com)
· [Release notes](https://github.com/SeqLog/SeqLog/releases/latest)

Free forever on macOS. No account, no trial, no upsell.

---

## What it does

- **Files are the truth** — plain `.md` with CommonMark and wikilinks. No database, no index,
  no proprietary layer. Open your notes in any editor, diff them in any tool.
- **Search without an index** — full text, backlinks, unlinked references and page names,
  through a Rust ripgrep FFI that greps the files each time. Nothing to rebuild, nothing to
  go stale.
- **Git is the sync layer** — embedded libgit2: auto-commit on save, push/pull to any remote,
  branch switching, conflict resolution, commit history with inline diffs, per-file revert.
- **Outliner, keyboard first** — indent, collapse, multi-select, drag to reorder across pages.
  Every edit has a symmetric inverse, so undo and redo never drift.
- **⌘K and ⌘F** — one palette for pages, slash commands, favorites and recents. One find bar
  that reaches across every journal day, loaded or not.
- **Full frame rate, any vault** — recycled rows drawn directly with CoreText, absolute-frame
  layout, no per-frame SwiftUI solve. A five-thousand-page journal flings like an empty one.

Also in the box: daily journal, tabs, drag & paste images, image lightbox, code fences,
unlinked references, favorites & recents, external-edit watching, GitHub sign-in,
signed auto-updates.

## Requirements

macOS 15 or later. Apple Silicon, or [an Intel build](https://seqlog.com/dl/latest/SeqLog-x86_64.dmg).
Signed and notarized; updates ship through Sparkle.

## Privacy

No account, no analytics, no telemetry. Your notes never leave your Mac.
[Full policy](https://seqlog.com/privacy).

## About this repository

This repo is SeqLog's **release home, update feed and issue tracker**. SeqLog is free, but it
is not open source — the application source is not published here.

- **Found a bug or want a feature?** [Open an issue](https://github.com/SeqLog/SeqLog/issues).
- **Looking for downloads?** [Releases](https://github.com/SeqLog/SeqLog/releases), or
  [seqlog.com](https://seqlog.com) for the always-current build.
- `appcast.xml` / `appcast-x86_64.xml` are the Sparkle update feeds. They are generated by the
  release pipeline; don't edit them by hand.

## iOS

An iOS version is in progress. It will be a paid app; macOS stays free forever.
